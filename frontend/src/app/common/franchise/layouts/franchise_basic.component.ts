import { Component, Input, Output, EventEmitter } from '@angular/core';

import jQuery from 'jquery';

@Component({
  selector: 'franchise_basic',
  templateUrl: 'franchise_basic.template.html',
})
export class franchise_basicComponent {}
